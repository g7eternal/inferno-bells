{
	"version": "0.2.0",
	"configurations": [
		{
			"command": "npm run dev",
			"name": "Run local dev",
			"request": "launch",
			"envFile": "${workspaceFolder}/.env",
			"type": "node-terminal"
		}
	]
}
